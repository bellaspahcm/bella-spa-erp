# 📔 2026-05-23 Global Progress Overview

> 🌟 **Daily Highlight**
> Cải tiến hoàn hảo giao diện Sidebar của BELLA SPA ERP với phối màu Pastel Pink (Hồng cánh sen/Dusky Rose) thời thượng sang trọng, giải quyết triệt để lỗi khoảng trắng chân trang do cơ chế co giãn màn hình (zoom bug) trên các trình duyệt Chromium, và tối ưu hóa độ tương phản sắc nét của Logo thương hiệu "Bella Spa" ở cả hai chế độ Light/Dark Mode. Đã triển khai thành công lên môi trường Production tại [https://bella-spa-erp.vercel.app](https://bella-spa-erp.vercel.app).

---

## 📁 Project Tracking

### 🔵 BELLA SPA ERP
* **Today's Progress**:
  * **Sửa lỗi khoảng trắng chân Sidebar**: Điều chỉnh chiều cao Sidebar từ `100vh` lên `h-screen md:h-[111.2vh]` để bù trừ chuẩn xác cho thuộc tính `zoom: 0.9` ở thẻ `html` trên màn hình cỡ trung (MD) trở lên, giúp Sidebar luôn kéo dài lấp đầy chân trang.
  * **Phối màu Premium Pastel Pink**: Thiết kế lại Sidebar với bảng màu hồng pastel đồng bộ:
    - *Light Mode*: Gradient từ hồng phấn nhẹ `#FFF5F7` đến hồng đào nhạt `#FCE4EC`, đường viền thanh nhã `#FBCFE8]/60`, các menu hoạt động hiển thị màu hồng sen sẫm `#BE185D`.
    - *Dark Mode*: Gradient từ hồng đất trầm ấm `#25131A` đến màu mận tối `#1A0C11`, đường viền `#3D1E2A`, các menu hoạt động có nền `#3D202E` và chữ hồng nhạt `#FFB7C5`.
  * **Độ tương phản của Logo thương hiệu**: Chuẩn hóa màu chữ viết tay của logo `Bella Spa` bằng inline style gán với biến CSS thương hiệu `--primary` (`style={{ color: 'var(--primary)' }}`), tự động phản hồi xuất sắc khi chuyển chế độ Sáng/Tối (hiển thị hồng đậm `#9D174D` ở Light Mode và hồng pastel `#FFB7C5` ở Dark Mode), giải quyết triệt để lỗi chữ đen bị chìm trên nền Dusky Rose tối màu.
  * **Thêm nút Đăng ký Chi Nhánh mới trên trang HQ Bella**: Tạo và thiết kế 2 nút bấm Đăng ký Chi Nhánh mới cực kỳ tinh tế, bóng bẩy (sử dụng gradient `from-rose-500 to-pink-600/65` kết hợp biểu tượng Plus) tại hai vị trí quan trọng của Dashboard HQ: một ở thanh điều hướng đầu trang (bên cạnh "Hồ sơ Spa Trụ sở") và một ở đầu bảng Danh sách chi nhánh hệ thống, liên kết trực tiếp đến trang kích hoạt `/signup`.
  * **Sửa lỗi Đăng ký Chi nhánh (Email Rate Limit Exceeded)**: Tích hợp giải pháp tạo tài khoản thông minh bằng Supabase Admin Client và Database RPC. Khi không tìm thấy biến môi trường `SUPABASE_SERVICE_ROLE_KEY` trên môi trường chạy (như Vercel Free plan hoặc local), Server Action `registerNewTenant` sẽ tự động chuyển sang gọi hàm RPC tùy chỉnh bảo mật `create_onboarding_user(...)` chạy với đặc quyền `SECURITY DEFINER` trên database. Hàm này trực tiếp tạo tài khoản và identity tương ứng trong schema `auth` và mã hóa mật khẩu bằng bcrypt qua `pgcrypto`, bỏ qua hoàn toàn luồng gửi thư SMTP của GoTrue, giải quyết triệt để lỗi rate limit và cho phép đăng ký mượt mà.
  * **Cải tiến giao diện Sidebar hoàn hảo theo cơ chế 2 vùng độc lập**: Loại bỏ hoàn toàn sự không thống nhất của Sidebar khi chuyển đổi chế độ tối:
    - **Chế độ Sáng (Light Mode)**: Ứng dụng dải màu hồng pastel đặc sắc, mềm mại, sang trọng và tinh tế: chữ Active màu hồng cánh sen `#BE185D`, chữ Inactive xanh tro hồng `#8A6D7C` ấm áp, tên Admin `#4C243B` kết hợp tag vai trò `#BE185D`. Đem lại trải nghiệm thị giác nhẹ nhàng như nhung đúng điệu Spa.
    - **Chế độ Tối (Dark Mode)**: Tự động chuyển đổi sang dải màu REGAL Thượng lưu `#1C1B19` đến `#11100F`, viền `#3E3A35`, chữ Logo vàng kim `#A67D44`, nút Active dùng nền `#5D1C34/30` với chữ kem `#EFE9E1` và điểm chỉ báo màu vàng đồng quý tộc. Giao diện tối hoàn toàn không bị ảnh hưởng.
  * **Thiết lập bảng màu REGAL Thượng lưu cho toàn bộ Giao diện Tối (Dark Mode)**: Dựa trên bảng màu phong cách "REGAL" tinh tế mà người dùng cung cấp, đã định cấu hình lại toàn bộ các biến CSS trong file `src/app/globals.css` dưới lớp phủ `.dark` và điều chỉnh `ThemeToggle.tsx`. Toàn bộ các thẻ card màu xám đục, nút bấm màu trắng chói sáng (`Xem chi tiết`), vòng avatar trắng, chữ slate mờ nhạt đều đã được chuyển hóa 100% sang:
    - **Nền chính (Background)**: `#11100F` (Charcoal Black sâu thẳm, đẳng cấp).
    - **Nền Card phụ**: `rgba(28, 27, 25, 0.6)` (Hợp chất bóng gương mờ sang trọng).
    - **Màu thương hiệu / Chủ đạo (Primary)**: `#5D1C34` (Deep Velvet Burgundy / Đỏ mận hoàng gia quý phái).
    - **Điểm nhấn nổi bật (Accent / Active)**: `#A67D44` (Bronze Gold / Đồng vàng kim thượng lưu).
    - **Vòng tròn Avatar & Huy hiệu**: Nền `#292623` chữ `#A67D44` cực kỳ cuốn hút.
    - **Nút hành động thường (Xem chi tiết)**: Nền tối `#1C1B19` viền `#3E3A35` chữ `#EFE9E1`.
    - **Văn bản & Tiêu đề**: `#EFE9E1` (Off-white / Trắng kem dịu mắt, độ tương phản tuyệt vời).
    - **Bản tin & Phụ đề**: `#CDBCAB` (Warm Beige / Be xám ấm áp).
    - **Đường viền & Input**: `#3E3A35` (Muted Warm Border).
    - Toàn bộ giao diện tối từ Card, Glassmorphism, Input, Scrollbar cho đến Theme Toggle đều tự động thừa hưởng đồng bộ hệ màu Thượng lưu này một cách hoàn mỹ mà không gây bất cứ biến đổi cấu trúc hay layout nào ở giao diện Sáng.
  * **Git & Vercel Production**: Gom toàn bộ thay đổi và commit đẩy trực tiếp lên Git branch `main`, đồng thời kích hoạt Vercel biên dịch bản build tối ưu hóa và phát hành bản cập nhật production mới nhất. Giao diện và tính năng hoạt động hoàn hảo.
* **Action Items**:
  * [x] Sửa lỗi giới hạn email (`email rate limit`) bằng cách chuyển đổi cơ chế đăng ký sang Admin Client kết hợp RPC Custom Auto-Confirm.
  * [x] Khắc phục triệt để lỗi crash của hàm RPC `create_onboarding_user` đối với các cột được tạo tự động (`confirmed_at`, `email`).
  * [x] Đóng kín lỗ hổng phân quyền (RLS) bằng migration chuyên biệt, ngăn chặn hoàn toàn việc rò rỉ hoặc xem chéo dữ liệu giữa các chi nhánh.
  * [x] Bổ sung chức năng tự Đổi mật khẩu cho người dùng hiện hành trong Cài đặt mà không làm thay đổi layout gốc.
  * [ ] Thu thập phản hồi thực tế từ chủ spa và khách hàng về tính thẩm mỹ của thanh Sidebar màu pastel mới.
  * [ ] Đánh giá và duy trì sự đồng bộ về màu sắc của các popup, modal và tooltip khác trong hệ thống để mang lại trải nghiệm thị giác nhất quán nhất.

---

## 🧠 Improvements & Learnings

📌 **New Rules / Discoveries**
* **Khắc phục lỗi co giãn chiều cao (Chromium Page Zoom Bug)**: Khi sử dụng thuộc tính `zoom` (ví dụ `zoom: 0.9`) trên thẻ `html` để thu phóng tỷ lệ hiển thị trang web, các phần tử có chiều cao cố định theo viewport `100vh` cũng sẽ bị tỷ lệ hóa tương đương vật lý (chỉ còn hiển thị khoảng `90vh`), tạo ra khoảng trống không mong muốn ở cuối trang. Công thức sửa lỗi là tăng chiều cao phần tử lên tỷ lệ đảo ngược: `100vh / zoom` (ở đây là `111.11vh`, chọn `111.2vh` để an toàn bao phủ).
* **Độ ưu tiên màu sắc (CSS Specificity) cho Logo**: Khi sử dụng logo dạng phông chữ viết tay cách điệu, việc gắn trực tiếp thuộc tính màu sắc thông qua inline styles liên kết với biến CSS `--primary` giúp đảm bảo độ ưu tiên cao nhất, khắc phục các xung đột thừa kế màu từ các thẻ bao quanh và duy trì độ sắc nét lý tưởng ở cả Light Mode lẫn Dark Mode.

---

## ✅ Global Action Items
- [ ] Giám sát hiệu năng và tốc độ phản hồi giao diện khi người dùng thao tác chuyển đổi nhanh giữa chế độ Sáng/Tối.
