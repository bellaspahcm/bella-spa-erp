{
  "buildCommand": "npm install --legacy-peer-deps && npm run build",
  "installCommand": "npm install --legacy-peer-deps --workspace=packages/* --include-workspace-root",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- ':!apps/mobile'"
}
