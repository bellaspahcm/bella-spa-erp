verification:
  tables:
    test_hc_patient_notes:
      columns:
        note_id: uuid
        patient_id: uuid
        tenant_id: uuid
        content: text
        created_at: timestamptz
      primary_key: [note_id]
      rls: required  # ← Declared as required but NOT enabled (violation)
