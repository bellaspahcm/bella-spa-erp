#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Bella Architecture Guard - Pre-commit Hook
# Enforces Healthcare Constitution boundaries before commit

echo "🔍 Running Bella Architecture Guard..."

# Run lint-staged for architecture rules
npx lint-staged

# Run Healthcare architecture guard
npm run healthcare:guard

# Exit code propagation: if any check fails, block commit
